exactly what data to gather from which source

generating datasets of each kind
corpus generator for rag
entire rag pipeline
observability across both pipelines

the visual for human eval + observing eval process + observing entire pipeline

next steps
- make it cheaper

the corpus should only be generated once, and added to it daily
the macro data should only be pulled once not on every ticker